/**
 * Re-export shim — SummaryCards was promoted to the shared module-page
 * scaffold kit (worklist #22). Import from '@/components/module-page' in new
 * code; this file only preserves existing import paths.
 */
export {
  SummaryCards,
  type SummaryItem,
} from '@/components/module-page/summary-cards'
