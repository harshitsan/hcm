/**
 * Re-export shim — RefreshButton / usePager / PagerControls were promoted to
 * the shared module-page scaffold kit (worklist #22). Import from
 * '@/components/module-page' in new code.
 */
export {
  RefreshButton,
  usePager,
  PagerControls,
} from '@/components/module-page/list-controls'
