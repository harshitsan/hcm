/**
 * Re-export shim — SortableHeader / selectColumn / SectionToolbar were
 * promoted to the shared data-table helpers (worklist #22). Import from
 * '@/components/common/data-table/table-helpers' in new code.
 */
export {
  SortableHeader,
  selectColumn,
  SectionToolbar,
} from '@/components/common/data-table/table-helpers'
